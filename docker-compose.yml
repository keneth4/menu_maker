services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /app
    command: >
      sh -c "if [ ! -f node_modules/.container-ready ]; then npm ci && touch node_modules/.container-ready; fi &&
      npm run dev -- --host 0.0.0.0 --port ${VITE_PORT:-5173}"
    ports:
      - "${VITE_PORT:-5173}:5173"
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "1"
      CHOKIDAR_INTERVAL: "300"
    volumes:
      - ./:/app
      - menumaker_node_modules:/app/node_modules
    networks:
      default:
        aliases:
          - menumaker-dev

  preview:
    profiles: ["preview"]
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "${PREVIEW_PORT:-4173}:4173"

  e2e:
    profiles: ["e2e"]
    image: mcr.microsoft.com/playwright:${PLAYWRIGHT_DOCKER_TAG:-v1.58.1-jammy}
    working_dir: /app
    environment:
      - E2E_GREP
    depends_on:
      - app
    entrypoint: ["/bin/bash", "-lc"]
    command:
      - >
        if [ ! -f node_modules/.container-ready ]; then npm ci && touch node_modules/.container-ready; fi &&
        if [ -n "$$E2E_GREP" ];
        then PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://menumaker-dev:5173 npm run test:e2e:local -- --grep "$$E2E_GREP";
        else PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://menumaker-dev:5173 npm run test:e2e:local;
        fi
    volumes:
      - ./:/app
      - menumaker_node_modules:/app/node_modules

volumes:
  menumaker_node_modules:
